#ifndef _CC_TYPE_H
#define _CC_TYPE_H 1

/* cc_isdigit: check if the character is a space */
extern int cc_isspace(char c);
/* cc_isdigit: check if the character is a digit */
extern int cc_isdigit(int src);

#endif
